<%= form_for(@list) do |f| %>
  <%= f.hidden_field :student_id, value: @student %>
  <label>Choose Classes</label><br>
  <%= f.collection_check_boxes :course_id, Course.all, :id, :name %><br>
  <%= f.submit %>
<% end %>